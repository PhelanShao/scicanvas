# Deep Thinking Backend - Environment Variables
# Rename this file to .env and fill in your API keys

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false

# LLM Provider API Keys (at least one required)
OPENAI_API_KEY=sk-xxx
ANTHROPIC_API_KEY=sk-ant-xxx
DEEPSEEK_API_KEY=sk-xxx

# Optional: Custom base URLs (for proxies or self-hosted models)
# OPENAI_BASE_URL=https://api.openai.com/v1
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# Optional: Redis cache
# REDIS_URL=redis://localhost:6379

# Optional: Default thinking depth (off, quick, standard, deep)
DEFAULT_THINKING_DEPTH=standard

# CORS Origins (comma-separated, or * for all)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
